export const FRAMEWORK_VERSION = "1.2.0";
export const MAP_FORMAT_VERSION = 1;

export function logFrameworkVersion(): void {
  console.info(`[3D SPACE LAB] Framework ${FRAMEWORK_VERSION} / Map Format ${MAP_FORMAT_VERSION}`);
}
